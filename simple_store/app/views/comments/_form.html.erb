<%= form_for([@article, Comment.new]) do |f| %>
<p>
<%= f.label :commenter %><br>
<%= f.text_field :commenter %>
</p>
<p>
	<%= f.label :content %><br>
	<%= f.text_area :content %>
</p>
<br />
<%= f.submit "Comment" %>
<% end %>
